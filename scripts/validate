#!/bin/sh
set -eu

echo "Checking formatting and lint..."
uv run ruff format --check custom_components tests scripts
uv run ruff check custom_components tests scripts

echo "Checking types..."
uv run mypy custom_components tests scripts

echo "Running tests..."
uv run pytest -q

echo "Validation passed."
